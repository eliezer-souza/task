type User {
  _id: String!
  name: String!
  username: String!
  email: String!
  password: String!
}

type Query {
  users: [User!]
}

type Mutation {
  createUser(user: UserInput): User!
}

input UserInput {
  name: String
  username: String
  email: String
  password: String
}
